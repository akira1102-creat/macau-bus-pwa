# Pinned upstream ref used by the local static-data synchronizer.
MACAU_BUS_DATA_REF=7f4520a4416415a4663d9afd0ad94da0c19d5b26
# Optional output location (defaults to public/data and data/upstream).
MACAU_BUS_DATA_DIR=public/data
# DSAT probe values are supplied on the command line; no credentials are stored here.
